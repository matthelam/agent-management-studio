---
name: tool-safety
description: Per-project CRUD-truthful guidance for tool calls. Verifies that operations claimed read-only really are, checks intent-to-command match, validates MCP/CLI versions, and enforces scoped access. Loaded by mode (audit/investigation modes load it strictly; fix/change/upgrade load it advisory). Generated by learn-codebase from the registry slice for {{PROJECT_NAME}}'s detected stack.
---

# tool-safety — {{PROJECT_NAME}}

Generated by `learn-codebase` from `tool-crud-profile.json` for the technologies
detected in this project. The CRUD-truthfulness data here is the project's
authoritative source for what each tool/method actually does — not what it
claims.

---

## When this skill is loaded

Per `config.json.modes.<active-mode>.tool_safety`:

| Mode | Stance | Behavior |
|---|---|---|
| `strict_read_only` (audit, investigation) | Loaded into context | Claude must consult before any tool call; operations classified as `write` or `read-with-side-effects` must NOT run. |
| `standard` (fix, change) | Loaded into context | Claude consults for ambiguous calls; advisory but expected. |
| `elevated` (upgrade) | Loaded into context | Stricter on destructive operations; standard otherwise. |

UserPromptSubmit hook injects the loading directive based on classified mode.

---

## CRUD profile for this project's toolset

This section is generated at seed time by `learn-codebase`, populated from the
slice of `registries/tool-crud-profile.json` matching the project's
`environment_snapshot`. Each entry classifies a method or command:

- **pure-read** — no side effects of any kind
- **read-with-side-effects** — claims read but writes logs, telemetry, lock
  files, mtime, cache, etc. Treat as write in `strict_read_only` mode.
- **write** — modifies state in the local repo or a remote system
- **destructive** — irreversible writes (deletions, force-pushes, drops)

### Tools

{{TOOL_CRUD_TABLE}}

*(Generator will fill this with per-tool/per-method rows from the registry
slice. Example rows:)*

| Tool / Method | CRUD class | Side effects |
|---|---|---|
| `git status` | pure-read | — |
| `git stash list` | read-with-side-effects | Touches reflog |
| `npm view <pkg>` | pure-read | — |
| `npm install --dry-run` | read-with-side-effects | May write package-lock.json artefacts in older npm versions |
| `git push --force` | destructive | Rewrites remote history |
| `mcp__atlassian__getJiraIssue` | pure-read | — |
| `mcp__atlassian__addCommentToJiraIssue` | write | Visible to all watchers |
| `mcp__azure__group_resource_list` | pure-read | — |
| `mcp__azure__deploy` | write (potentially destructive) | Modifies live infrastructure |

---

## Intent-to-command match

Before executing a tool call, Claude verifies the command matches the dev's
stated intent:

1. **Stated intent** (from current prompt or active deliver-work step) — what
   the dev/agent has declared they want to do.
2. **Allowed CRUD scope** — derived from intent + active mode:
   - "investigate" / "audit" / "trace" → `pure-read` only
   - "fix" / "change" → `pure-read`, `read-with-side-effects`, `write` allowed; `destructive` requires explicit confirmation
   - "deploy" / "release" → all classes; `destructive` requires confirmation
3. **Match check** — does the proposed command's CRUD class fall within the
   allowed scope for this intent? If not, surface mismatch and ask for
   confirmation OR refuse.

---

## Version verification

For tools that have version-conditional behaviour, the registry records the
expected versions for this project (per `environment_snapshot.frameworks` etc.).
Before invoking a versioned tool, verify the active version matches; if not,
warn the dev of behaviour-difference risk.

{{VERSION_CHECKS}}

*(Generator fills this with per-tool version-check commands.)*

---

## Scoped access

The project's `approaches.md` GUARD RAILS declare paths/resources off-limits
or requiring approval. Mirrored here as a lookup-table for fast pre-tool checks
(actual hard-blocking is done by the `prescriptive-rules-block.sh` PreToolUse
hook reading `prescriptive-rules.json`):

{{SCOPED_ACCESS_TABLE}}

---

## Failure mode

If Claude proposes a tool call that violates this skill's rules:

1. Surface the violation explicitly: *"This call is `<class>`. Mode allows
   `<allowed-classes>`. Refusing."*
2. Suggest an alternative if one exists (e.g. *"Use `git status` instead of
   `git stash list` for a pure-read."*).
3. If the dev insists, require explicit override (which is logged via
   `audit/service.md` as `tool_safety_override`).

---

## Provenance

Generated by `learn-codebase` on {{TIMESTAMP}} from:
- `registries/tool-crud-profile.json` ({{TOOL_PROFILE_VERSION}})
- `registries/tech-tool-map.json` ({{TECH_MAP_VERSION}})
- Project's `config.json.environment_snapshot`

Re-run `update` to refresh this skill if registry entries or stack change.
