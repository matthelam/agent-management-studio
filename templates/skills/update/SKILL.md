---
name: update
description: Refresh the project's prescriptive layer — environment drift (dependency versions, configs) and behavioural drift (claude-mem session evidence). Use when patterns/approaches feel stale, after a dependency upgrade, when claude-mem has accumulated new evidence, or as a routine check. Supports `--preview` for a read-only diff.
---

# update

{{INLINE: procedures/update.md}}
