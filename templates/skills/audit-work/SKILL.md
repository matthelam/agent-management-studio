---
name: audit-work
description: Read-only investigation across the git repo. Single-ticket mode (trace one work item through its lifecycle — "why did this happen?") or cross-ticket mode (pattern audit across multiple work items — "are we consistently following the auth pattern?"). Uses claude-mem session evidence + work-item logs. Produces structured findings; never modifies anything.
---

# audit-work

{{INLINE: procedures/audit-work.md}}
